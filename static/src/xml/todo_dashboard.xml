<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="todo_app.Dashboard">
        <div class="o_action p-4 bg-light h-100 overflow-auto">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="mb-0">Todo Dashboard</h2>
                <div>
                    <button class="btn btn-primary me-2" t-on-click="createQuickTask">Nueva Tarea</button>
                    <button class="btn btn-light border" t-on-click="refresh"><i class="fa fa-refresh"/></button>
                </div>
            </div>

            <t t-if="state.loading">
                <div class="text-center p-5"><i class="fa fa-spinner fa-spin fa-3x"/></div>
            </t>
            <t t-else="">
                <div class="row g-3 mb-4">
                    <div class="col-md-3">
                        <div class="card shadow-sm text-center p-3 border-0" t-on-click="() => this.openTasks([])" style="cursor:pointer">
                            <div class="text-muted small fw-bold">TOTAL</div>
                            <div class="display-6 text-primary" t-esc="state.total"/>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card shadow-sm text-center p-3 border-0 border-start border-warning border-4" t-on-click="() => this.openTasks([['is_done','=',false]])" style="cursor:pointer">
                            <div class="text-muted small fw-bold">PENDIENTES</div>
                            <div class="display-6 text-warning" t-esc="state.pending"/>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card shadow-sm text-center p-3 border-0 border-start border-success border-4" t-on-click="() => this.openTasks([['is_done','=',true]])" style="cursor:pointer">
                            <div class="text-muted small fw-bold">HECHAS</div>
                            <div class="display-6 text-success" t-esc="state.done"/>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card shadow-sm text-center p-3 border-0 border-start border-danger border-4">
                            <div class="text-muted small fw-bold">PROGRESO</div>
                            <div class="display-6 text-danger"><t t-esc="state.completion_rate"/>%</div>
                        </div>
                    </div>
                </div>

                <div class="row g-3">
                    <div class="col-md-6">
                        <div class="card shadow-sm p-4 border-0 h-100">
                            <h5 class="card-title">Distribución por Prioridad</h5>
                            <div style="position: relative; height: 300px;">
                                <canvas id="priorityChart"/>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="card shadow-sm p-4 border-0 h-100 d-flex flex-column justify-content-center align-items-center">
                            <t t-if="state.completion_rate >= 100">
                                <div class="text-center">
                                    <div class="display-1">🏆</div>
                                    <h3>¡Todo completado!</h3>
                                    <button class="btn btn-success btn-lg mt-3" t-on-click="celebrate">Lanzar Confeti</button>
                                </div>
                            </t>
                            <t t-else="">
                                <div class="progress w-100 mb-3" style="height: 30px;">
                                    <div class="progress-bar progress-bar-striped progress-bar-animated bg-info" 
                                         t-attf-style="width: {{state.completion_rate}}%"/>
                                </div>
                                <p class="text-muted">¡Sigue así! Te faltan <t t-esc="state.pending"/> tareas.</p>
                            </t>
                        </div>
                    </div>
                </div>
            </t>
        </div>
    </t>
</templates>